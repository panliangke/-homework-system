# 作业提交系统 - 部署指南

## 项目结构

```
作业提交系统/
├── index.html          # 前端页面
├── style.css           # 样式
├── app.js              # 前端逻辑
├── api/proxy.js        # Vercel 云函数（API 代理）
├── webapp.py           # Python Web 应用（静态文件 + API 代理，Render 用）
├── server.py           # 本地开发服务器
├── requirements.txt    # 依赖（仅标准库，无需安装）
├── render.yaml         # Render 部署配置
├── vercel.json         # Vercel 部署配置
├── 启动.bat            # 本地一键启动
└── 配置防火墙.bat      # 局域网访问防火墙配置
```

---

## 方式一：本地运行（仅自己可用）

双击 `启动.bat`，浏览器访问 `http://localhost:8080`

---

## 方式二：局域网访问（同一 WiFi 下的同学可用）

1. 右键 `配置防火墙.bat` → 以管理员身份运行
2. 双击 `启动.bat`
3. 把启动窗口显示的局域网地址发给同学，如 `http://10.129.192.134:8080`

---

## 方式三：部署到公网（所有人都可访问）

### 推荐：Render.com（免费，支持最大 100MB 文件上传）

本系统需要上传作业文件（最大 100MB），Render 对请求大小没有严格限制，
因此作为首选平台。

**步骤：**

1. **注册账号**
   - 打开 https://render.com
   - 用 GitHub 账号登录（没有就先注册 GitHub）

2. **创建 GitHub 仓库**
   - 在 https://github.com/new 新建一个**公开仓库**（免费账号只能公开）
   - 把本文件夹里的所有文件上传到仓库根目录，至少包括：
     `index.html`、`style.css`、`app.js`、`webapp.py`、
     `requirements.txt`、`render.yaml`
   - 上传方式（任选一种）：
     - 网页端：仓库页面点 "Add file" → "Upload files"，拖拽上传
     - 命令行：
       ```bash
       git init
       git add .
       git commit -m "作业提交系统"
       git branch -M main
       git remote add origin https://github.com/<你的用户名>/<仓库名>.git
       git push -u origin main
       ```

3. **部署到 Render**
   - 在 Render 控制台点击 "New +" → "Blueprint"
   - 连接并选中你的 GitHub 仓库
   - Render 会自动读取 `render.yaml`，无需修改任何配置，直接点 "Apply"
   - 等待 2–3 分钟，部署完成后会得到公网地址，
     形如 `https://homework-system-xxxx.onrender.com`

4. **分享链接**
   - 把 Render 给的地址发给任何人，他们都能访问
   - 免费实例 15 分钟无人访问会休眠，首次打开可能要等约 50 秒，属正常现象
   - 以后只要 `git push` 更新仓库，Render 会自动重新部署

### 备选：Vercel（打开更快，但不能传大文件）

Vercel 免费版云函数请求体上限约 **4.5MB**，超过会上传失败；
如果作业文件都很小，可选它，冷启动比 Render 快。

1. 打开 https://vercel.com，用 GitHub 账号登录
2. 点击 "Add New" → "Project"，导入上面的 GitHub 仓库
3. 无需填写任何配置（`vercel.json` 已配好），直接点 "Deploy"
4. 约 1 分钟后得到 `https://<项目名>.vercel.app` 公网地址

### 平台对比

| 平台 | 免费额度 | 大文件上传 | 冷启动 | 说明 |
|------|---------|-----------|--------|------|
| Render | 750 小时/月 | ✅ 支持 100MB | 较慢（休眠后约 50 秒） | 推荐 |
| Vercel | 流量充足 | ❌ 限 4.5MB | 很快 | 仅适合小文件 |
| Railway | 小额试用额度 | ✅ | 较快 | 免费额度有限 |

> 中国大陆访问 `onrender.com` / `vercel.app` 的免费域名可能偏慢或不稳定，
> 如遇此问题，可在平台上绑定自己的域名解决。

---

## 安全提醒

- **不要把任何真实学号、密钥写进代码或文档**，公开仓库里的内容任何人都能看到。
- 本系统登录凭据由教学后端（`homework.pkuai.cc`）统一管理，
  学生凭自己的学号和老师发放的密钥登录。

## 登录账号

- 使用老师发放的**学号**和**密钥**登录（如计概C2班等开课班级）。
